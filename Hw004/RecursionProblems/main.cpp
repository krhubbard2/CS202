// Kelby Hubbard
// CS202
// March 8, 2020
// Hw004 -- Recursion Problems
#include <catch2/catch.hpp>
#define CATCH_CONMFIG_MAIN

int fib(int n)
{
  //

  return 0;
}

int factorial(int n)
{
  //

  return 0;
}

int ack(int m, int n)
{
  //

  return 0;
}

TEST_CASE("Fibonacci Sequence", "[Fibonacci]")
{
    REQUIRE(fib(0) == 0);
    REQUIRE(fib(1) == 1);
    REQUIRE(fib(9) == 34);
    REQUIRE(fib(14) == 377);
}
